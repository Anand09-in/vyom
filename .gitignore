# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Secrets — NEVER commit .env
.env

# Data (raw PDFs and CSVs belong in S3, not git)
data/
*.sqlite
mlruns/

# Eval run artifacts — churn every run, not source of truth
eval/results*.json

# OS / editor
.DS_Store
.idea/
.vscode/

# Terraform state (contains secrets)
infra/.terraform/
*.tfstate
*.tfstate.*
*.tfvars

# Node
node_modules/
.next/